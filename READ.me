1. Create a virtual envoirment by running "python3 -m venv venv"
2. run "source venv/bin/activate"
3. Install the following by running "pip install mysql-connector-python tabulate"
4. in the python script, change the login credintials to your database.
5. calling the function "See Statistics" will return an error for the first time, its python related error, shouldnt be a problem if u run the function again.
